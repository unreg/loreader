import * as types from '../../actions/action-types';


const initialState = {
  TrackerData: {
  }
}


const TrackerReducer = function(state=initialState, action) {
  switch(action.type) {

    default: return state;
  }
}


export default TrackerReducer;
